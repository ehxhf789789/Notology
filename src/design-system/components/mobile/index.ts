/**
 * Canonical mobile primitive barrel (Stage 5.0.10c).
 *
 * All mobile-specific primitives live here. The corresponding
 * `src/features/mobile/...` files are now thin re-export shims kept
 * for backward compatibility with existing import sites.
 */
export { BottomSheet } from './BottomSheet';
export { TabBar, type TabId } from './TabBar';
export { FAB } from './FAB';
export { ActionSheet } from './ActionSheet';
export { SwipeableRow } from './SwipeableRow';
