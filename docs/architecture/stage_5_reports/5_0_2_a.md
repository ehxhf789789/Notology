# Stage 5.0.2a — Primitive Component Library (simple primitives)

**Date**: 2026-05-14
**Status**: ✅ Acceptance criteria met — ready for HanBin visual sign-off
**Parent plan**: [`STAGE_5_0_DESIGN_SYSTEM_PLAN.md`](../STAGE_5_0_DESIGN_SYSTEM_PLAN.md) §3, §16
**Predecessor**: [5.0.1 design tokens](./5_0_1.md) (commit `7637031`)

---

## 1. Why 5.0.2 split into a + b

Plan §3 lists 23 primitives. Plan §14 estimates 5.0.2 at 2 sessions. To keep
each commit small and reviewable, 5.0.2 lands in two halves:

| Half | Components | Dependency |
|---|---|---|
| **5.0.2a (this commit)** | 14 simple primitives, pure HTML+CSS | none |
| **5.0.2b (next session)** | 9 complex primitives | Floating-UI for positioning, focus-trap utility, portal helpers |

5.0.2a is the foundation 5.0.2b extends.

---

## 2. The 14 primitives landed

Files live in `src/design-system/components/`, one `.tsx` each + a single
shared `primitives.css`. Importing any primitive pulls the stylesheet in
once via the barrel `index.ts` side-effect import.

| File | Component | Variants / sizes | Notes |
|---|---|---|---|
| `Button.tsx` | `<Button>` | variant: `primary` / `secondary` / `ghost` / `danger`; size: `sm` / `md` / `lg` | left/right icons, loading spinner, `fullWidth`, forwardRef |
| `IconButton.tsx` | `<IconButton>` | variant: `ghost` / `subtle` / `primary`; size: `sm` / `md` / `lg` | required `aria-label`, `pressed` (toggle), forwardRef |
| `Input.tsx` | `<Input>` | size: `sm` / `md` / `lg` | left/right icons, `invalid`, native `size` exposed as `htmlSize` to avoid collision |
| `Textarea.tsx` | `<Textarea>` | — | `invalid`, `noResize` |
| `Checkbox.tsx` | `<Checkbox>` | — | label + description; `indeterminate` |
| `Radio.tsx` | `<Radio>` | — | label + description |
| `Toggle.tsx` | `<Toggle>` (switch) | size: `sm` / `md` | label + description; sets `role="switch"` by default |
| `Spinner.tsx` | `<Spinner>` | size: `sm` / `md` / `lg` | a11y `aria-label`; reduced-motion slows to 4s |
| `Skeleton.tsx` | `<Skeleton>` | variant: `rect` / `text` / `circle`; radius: `sm` / `md` / `lg` / `full` | shimmer animation; freezes under `prefers-reduced-motion` |
| `ProgressBar.tsx` | `<ProgressBar>` | variant: `default` / `success` / `warning` / `danger` | indeterminate when `value=null`; required `aria-label` |
| `Badge.tsx` | `<Badge>` | variant: `neutral` / `accent` / `success` / `warning` / `danger` / `info`; size: `sm` / `md` | `outline`, `dot` (status), `icon` |
| `Card.tsx` | `<Card>` | density: `compact` / `cozy` / `spacious` | `interactive` (hover/focus/press), `selected`, `as` (div/article/section/li) |
| `EmptyState.tsx` | `<EmptyState>` | — | `icon`, `title`, `description`, `action` slot (typically `<Button>`) |
| `KeyboardHint.tsx` | `<KeyboardHint>` | size: `sm` / `md` | renders `<kbd>` per key with chord separator; arrow/escape/enter glyph mapping |

**Total**: 14 .tsx files + 1 primitives.css + 1 index.ts = 16 files in `src/design-system/components/`.

API conventions:

- **forwardRef** on Button / IconButton / Input / Textarea / Checkbox / Radio / Toggle / Card — these benefit from ref access for focus management.
- **Native attribute pass-through** via `{...rest}` so every primitive accepts standard HTML props (`onClick`, `aria-*`, `data-*`).
- **a11y by construction**: every interactive primitive has built-in `:focus-visible` ring (`--bdr-focus`); `IconButton` requires `aria-label`; `Toggle` defaults to `role="switch"`; `Spinner`/`ProgressBar` get `role="progressbar"` + `aria-valuetext`.
- **Class naming**: `ds-*` prefix on every primitive class — easy grep, no risk of collision with existing 70+ CSS files.

---

## 3. CSS scoping discipline

`src/design-system/components/primitives.css` (~600 lines) is the **only**
new stylesheet. It references **Tier 2 + Tier 3 tokens only** — verified by
`npm run audit:tokens`:

```
Tier-1 primitive leakage: PASS — 0 references outside the source files
```

To make this work, two new semantic tokens were added to `tokens.css` in
this same commit (Tier 2 aliases for Tier 1 primitives — see §4):

- `--space-px` (1px), `--space-2xs` (2px) — for hairlines / tight padding
- `--fw-regular/medium/semibold/bold` — semantic font-weight aliases
- `--font-sans`, `--font-display` — semantic font-family aliases (previously only `--font` alias existed)

`primitives.css` adds **0 hex literals** and **0 raw margin/padding px** to
the audit baseline (held at 815 / 901 across 75 CSS files).

---

## 4. tokens.css additions (in this commit)

Small, additive — no breaking changes:

```css
/* Tier 1 */
--primitive-space-px: 1px;
--primitive-space-half: 2px;

/* Tier 2 — semantic font weight */
--fw-regular:  var(--primitive-fw-400);
--fw-medium:   var(--primitive-fw-500);
--fw-semibold: var(--primitive-fw-600);
--fw-bold:     var(--primitive-fw-700);

/* Tier 2 — semantic font family */
--font-sans:    var(--primitive-font-sans);
--font-display: var(--primitive-font-display);
/* --font-mono already exists in legacy alias block */

/* Tier 2 — hairline spacing */
--space-px:  var(--primitive-space-px);
--space-2xs: var(--primitive-space-half);
```

These are forward-looking — Tier 2 names that didn't exist in 5.0.1 because
no consumer needed them yet. 5.0.2a's primitives surfaced the gap.

---

## 5. Visual verification — primitives-preview.html

`src/design-system/components/primitives-preview.html` is a static page
showing every primitive in every variant + size, side-by-side, with
light / system / dark toggle buttons. Pure HTML — uses the same DS class
names the React components emit.

**To verify**:

```
open  src/design-system/components/primitives-preview.html   # macOS/Linux
start src/design-system/components/primitives-preview.html   # Windows
```

Coverage:

- **Button**: all 4 variants × 3 sizes, plus disabled / icon-prefixed / icon-suffixed / loading states
- **IconButton**: 3 variants × 3 sizes, plus `aria-pressed=true` and disabled
- **Input + Textarea**: 3 sizes, with-icon, invalid, disabled
- **Checkbox + Radio + Toggle**: plain / checked / with-description / disabled
- **Spinner**: 3 sizes
- **Skeleton**: text + circle + rect variants
- **ProgressBar**: determinate (3 variants) + indeterminate
- **Badge**: 6 variants × 2 sizes + outline + dot
- **Card**: 3 densities × interactive × selected combos
- **EmptyState + KeyboardHint**: standard usage

---

## 6. Acceptance criteria checklist

- [x] 14 primitive components implemented in `src/design-system/components/`
- [x] Single `primitives.css` consuming only Tier 2 / Tier 3 tokens
- [x] Tier-1 leakage audit: **PASS** (0 outside whitelisted source files)
- [x] Baseline hex / margin-padding metrics unchanged (815 / 901)
- [x] Barrel `index.ts` with named exports + side-effect CSS import
- [x] `npx tsc --noEmit` clean (no errors)
- [x] `primitives-preview.html` covers every primitive × every variant
- [x] a11y: every interactive primitive has `:focus-visible` ring; required `aria-label` on `IconButton`; `role="switch"` on `Toggle`; `role="progressbar"` on `Spinner` + `ProgressBar`
- [x] Reduced-motion: `Spinner` slows to 4s, `Skeleton` freezes, `ProgressBar` indeterminate stops
- [x] **Zero changes to existing surfaces** — primitives are net-new, no existing component consumes them yet (5.0.3+ migrates surfaces)
- [x] This report written
- [ ] **HanBin visual sign-off** ← pending preview HTML inspection

---

## 7. Decisions noted for follow-up

| Decision | Where it shows up later |
|---|---|
| `<Card as="...">` accepts `div` / `article` / `section` / `li` — kept narrow to avoid full polymorphic generic at this stage. Expand if 5.0.7 search rows need more. | 5.0.7 |
| `<KeyboardHint>` arrow / escape / enter glyph mapping is local (10 entries). 5.0.4-pre command audit may centralize this into a shortcuts helper. | 5.0.4-pre |
| `<Input>` renames native `size` to `htmlSize` to free the prop for visual size. None of the existing surfaces use the native `size` attribute, so this rename is safe. | — |
| Toggle's `role="switch"` is default-on. Settings that historically used checkboxes for boolean preferences can opt back to `role="checkbox"` via prop override. | 5.0.6 |
| No Toast / Tooltip / Popover / Dialog / DropdownMenu / Tabs / SegmentedControl / Select yet — those land in 5.0.2b with Floating-UI. | 5.0.2b |

---

## 8. Next sub-stage entry conditions

### 5.0.2b — Floating-UI dependent primitives

Begins when HanBin signs off on this preview HTML. Scope:

1. `npm install @floating-ui/react` (Q6 default-accepted, ~30 KB)
2. Implement `<Tooltip>`, `<Popover>`, `<DropdownMenu>`, `<ContextMenu>` against Floating-UI's React adapter
3. Implement `<Dialog>` with focus-trap + ESC/backdrop dismissal + portal mount (no Floating-UI but lives alongside)
4. Implement `<Toast>` as primitive promotion of existing `features/shared/Toast.tsx` (single Toaster + queue API)
5. Implement `<Tabs>` and `<SegmentedControl>` (state + keyboard nav)
6. Implement `<Select>` (Floating-UI listbox positioning)

Estimated: 1 session.

### 5.0.3 — App shell

Entry condition: 5.0.2a + 5.0.2b both signed off. Begins consuming
primitives to redesign TitleBar / Sidebar / RightPanel / hover chrome.
This is where the calendar discoverability issue (diagnosed earlier in
this session) gets resolved naturally — RightPanel becomes a tab-row
with Calendar as the first visible tab.

---

## 9. Files in this commit

```
A docs/architecture/stage_5_reports/5_0_2_a.md
A src/design-system/components/Badge.tsx
A src/design-system/components/Button.tsx
A src/design-system/components/Card.tsx
A src/design-system/components/Checkbox.tsx
A src/design-system/components/EmptyState.tsx
A src/design-system/components/IconButton.tsx
A src/design-system/components/Input.tsx
A src/design-system/components/KeyboardHint.tsx
A src/design-system/components/ProgressBar.tsx
A src/design-system/components/Radio.tsx
A src/design-system/components/Skeleton.tsx
A src/design-system/components/Spinner.tsx
A src/design-system/components/Textarea.tsx
A src/design-system/components/Toggle.tsx
A src/design-system/components/index.ts
A src/design-system/components/primitives.css
A src/design-system/components/primitives-preview.html
M src/design-system/tokens.css                    (Tier 1 hairline + Tier 2 fw/font/space aliases)
```

No backend changes, no surface migrations, no other-track files touched.

---

## 10. Korean + font-change verification (follow-up patch, 2026-05-15)

HanBin's review of the initial preview: "모든 디자인이 영어 초안으로 보여준
거라, 한글 폰트를 고려한 건지, 그리고 폰트 변경도 고려한건지 의문."

### Audit findings

| Layer | State |
|---|---|
| Production `--app-font` system ([`reset.css:14-18`](../../../src/styles/base/reset.css)) | ✅ already Korean-aware (5 font choices, all with Pretendard/Malgun Gothic/NanumGothic/Noto Sans KR fallbacks) |
| `[data-font="..."]` runtime switching via `settingsStore.setFont()` | ✅ already wired |
| Primitive `.ds-*` components | ✅ correct — `font-family: inherit` on `Button` / `Input` / `Textarea`; others omit `font-family` and inherit from the `*` selector in reset.css → automatically receive `--app-font` |
| Tier 2 `--font-sans` semantic token | ❌ was Latin-prioritized — missing Malgun Gothic / Gulim fallbacks |
| `token-preview.html` + `primitives-preview.html` | ❌ used `var(--primitive-font-sans)` directly, didn't import reset.css, had no `data-font` toggle, mostly English labels |

So the **runtime system was correct** — the **preview pages** failed to demonstrate it. Fix is mechanical, no architectural change.

### Patch

1. **`tokens.css`**: rewrote `--primitive-font-sans` + `--primitive-font-display` with Korean-first fallback chain (`Pretendard → Malgun Gothic → 맑은 고딕 → Apple SD Gothic Neo → -apple-system → BlinkMacSystemFont → Segoe UI → Inter → system-ui`). Added `D2Coding` to `--primitive-font-mono`.
2. **Both preview HTMLs**:
   - Set `html lang="ko" data-font="default"` (matches production cold-start state)
   - Import `../../styles/base/reset.css` so the same `--app-font` cascade applies as in the live app
   - `body` now uses `font-family: var(--app-font)` (was `var(--primitive-font-sans)`)
   - Added a font-picker `<select>` in the toolbar with all 4 built-in choices (default / nanum / noto / malgun) wired to `[data-font="..."]` on `<html>` — matches `settingsStore.setFont()` behavior 1:1
   - Localized component labels: every Button / IconButton aria-label / Input placeholder / Checkbox-Radio-Toggle label uses Korean (or 한글 + 영문 병기)
3. **token-preview.html**: added §"한글 가독성 검증 — line-height 차이" (same Korean sentence at `--lh-tight` / `--lh-normal` / `--lh-relaxed` side-by-side) + §"폰트 비교 매트릭스 — 같은 문장, 4종 폰트" (forced font-family override per row so HanBin can see how each font renders even without changing the picker)
4. **primitives-preview.html**: added §"폰트 비교 매트릭스 — 컴포넌트 안에서의 한글 렌더링" (Button + Input rendered under each of the 4 fonts) — direct verification that primitives respect font selection via inherit

### Files in this follow-up patch

```
M src/design-system/tokens.css                        (Tier 1 font-sans/display Korean fallback)
M src/design-system/token-preview.html                (reset.css + data-font + Korean samples)
M src/design-system/components/primitives-preview.html (same + Korean labels everywhere)
M docs/architecture/stage_5_reports/5_0_2_a.md        (this section)
```

### Verification

- `npm run audit:tokens`: still PASS, Tier-1 leakage = 0, baseline 815/901 held
- Production app behavior unchanged — `--app-font` system was already correct; we only Korean-ized the static `--font-sans` fallback (used by ~zero consumers today; future-proofing)
- Preview pages now demonstrate the full font-change cycle. HanBin verification path:
  1. Open `primitives-preview.html` → confirm Korean labels render cleanly in default font (Pretendard if installed, Malgun Gothic otherwise)
  2. Use font-picker dropdown → switch through nanum / noto / malgun → every primitive's font follows immediately
  3. Toggle theme to dark → repeat font switching → both axes orthogonal as designed
